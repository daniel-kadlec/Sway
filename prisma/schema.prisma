generator client {
  provider = "prisma-client"
  output   = "../lib/generated"
}

datasource db {
  provider = "postgresql"
}

enum Stage {
  BACKLOG
  PRIMARY_CONTACT
  PRIMARY_CONTACT_FOLLOW_UP
  SECONDARY_CONTACT
  SECONDARY_CONTACT_FOLLOW_UP
  CLOSED
}

enum LeadStatus {
  IDLE
  ACTIVE
  PENDING
  CLOSED
}

enum LeadOutcome {
  WON
  LOST
}

enum LeadLossReason {
  GHOSTED
  REJECTED
  NO_BUDGET
  NO_RESPONSE
  OTHER
}

enum Platform {
  EMAIL
  INSTAGRAM
  PHONE
}

enum LogType {
  CREATED
  STAGE_CHANGED
  STATUS_CHANGED
  CLOSED
  RESET
}

model Lead {
  id          String  @id @default(uuid())
  companyName String
  note        String?

  primaryContactValue String?
  primaryPlatform     Platform?

  secondaryContactValue String?
  secondaryPlatform     Platform?

  website String?

  stage   Stage
  status  LeadStatus      @default(ACTIVE)
  outcome LeadOutcome?
  reason  LeadLossReason?

  nextActionAt DateTime?

  primaryContactAt    DateTime?
  primaryFollowUpAt   DateTime?
  secondaryContactAt  DateTime?
  secondaryFollowUpAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  logs LeadLog[]

  @@index([stage])
  @@index([status])
  @@index([nextActionAt])
  @@index([updatedAt])
}

model LeadLog {
  id     String @id @default(uuid())
  leadId String
  lead   Lead   @relation(fields: [leadId], references: [id], onDelete: Cascade)

  type LogType

  fromStage Stage?
  toStage   Stage?

  fromStatus LeadStatus?
  toStatus   LeadStatus?

  outcome LeadOutcome?
  reason  LeadLossReason?

  nextActionAt DateTime?

  createdAt DateTime @default(now())
}

model Settings {
  id String @id @default(uuid())

  contactDelay            Int @default(7)
  advanceFromBacklogDelay Int @default(0)
}
